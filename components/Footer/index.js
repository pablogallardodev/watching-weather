import styles from "styles/footer.module.css";

const Footer = () => {
  return (
    <div className={styles.footer}>
      <label>Hello I am a Footer👋!!</label>
    </div>
  );
};

export default Footer;
